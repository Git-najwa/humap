{
  "name": "humap-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "humap-backend",
    "humap-frontend"
  ],
  "scripts": {
    "start": "npm --prefix humap-backend start",
    "test": "npm --prefix humap-backend test"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  }
}
